# 각 워크스페이스별 .env 위치:
# - apps/server/.env  (apps/server/.env.example 참고)
# - apps/mobile/.env  (apps/mobile/.env.example 참고)
#
# 로컬 인프라(PostgreSQL + Redis)는 docker-compose로 실행:
#   docker compose up -d
